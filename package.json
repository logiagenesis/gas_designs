{
  "name": "gas-designs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "pages": "node scripts/build-pages.mjs",
    "brand": "node scripts/build-brand.mjs",
    "prebuild": "npm run brand && npm run pages",
    "build": "vite build",
    "dev": "npm run prebuild && vite",
    "preview": "vite preview",
    "check:contrast": "node scripts/check-contrast.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8"
  }
}
