{
  "name": "gas-designs",
  "version": "2.0.0",
  "private": true,
  "description": "Gas Designs — gas installation, maintenance and compliance. Static site.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/optimise-images.mjs",
    "preview": "vite preview",
    "optimise:images": "node scripts/optimise-images.mjs",
    "check:contrast": "node scripts/check-contrast.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8"
  }
}
